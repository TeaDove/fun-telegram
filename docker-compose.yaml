version: '3.1'

services:
  client:
    restart: always
    environment:
      fun_file_storage_path: "/config"
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - "~/.config/fun-telegram:/config"
      - ".env:/.env"
